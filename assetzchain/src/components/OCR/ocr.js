import React from 'react';

const OCR = () =>{
    return(
        <nav style={{display: 'flex', justifyContent: 'flex-end'}}>
            <p> Sign Out </p>
        </nav>
    );
}
export default OCR;
